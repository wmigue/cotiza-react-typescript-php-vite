# COMO SUBIR EL BUILD
- Tener en cuenta la ruta de los archivos transpilados, en el html modificar la ruta si es necesario o causa algun error.
- el backend subirlo en el mismo directorio en /src/backend
- la cadena de conexion del los archivos del backend tiene un nivel menos ../